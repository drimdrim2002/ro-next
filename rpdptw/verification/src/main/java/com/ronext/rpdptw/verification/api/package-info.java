/**
 * Independent verification contract ownership.
 *
 * <p>This module depends on core only; it must never depend on solver, search
 * caches, provider SDKs or the legacy placeholder.</p>
 */
package com.ronext.rpdptw.verification.api;
