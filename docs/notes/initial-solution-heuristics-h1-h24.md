---
title: 초기해 구축 25개 알고리즘 — 목적·구현·실측 (설명 노트, 계약 아님)
date: 2026-09-04
sources:
  - ../implementation/stage-04-initial-solution-heuristics.md (§5 의사코드 = 계약)
  - ../implementation/stage-04-initial-solution-heuristics-survey.md (§2 근거 · §2.5 실물 실측 · §5 비교 프로토콜)
  - ../implementation/stage-04-alns.md (§4 repair 연산자 — §4.1 "ALNS가 하는 일과 같은 계열" 판단 근거)
revisions:
  - 2026-09-05 H22·H2·H14·H18 코드 해설 노트 링크 — §6.1 squeaky-wheel, §6.2 urgency-regret3, §6.3 vehicle-fill-remaining-regret, §6.4·§6.5 hilbert-split
  - 2026-09-05 H22·H2·H14·H18 코드 해설 노트 링크 — §6.1 squeaky-wheel, §6.2 urgency-regret3, §6.3 vehicle-fill-remaining-regret, §6.4·§6.5 hilbert-split
  - 2026-09-04 H25 `zone-quota-exchange-fill` 편입 반영 — §2 끝에 H25 설명, §4.2 축 ① 후보에 H25,
    §4.4에 H23 기권 시 대체 슬롯 문구. 24개 → **25개**. §1 실측표는 H25 편입 전 실행이라 그대로 둔다
  - 2026-09-04 §4 병렬 포트폴리오 선정 검토(5개 + 조건부 1개)·§5 25개 밖의 방향 추가. §3 마지막 항목 문구 정정
  - 2026-09-04 §6 포트폴리오 4개(H22·H2·H14·H18) 예시 설명 + §6.0 삽입 비용의 정의(차량 크기 미포함) + §6.5 힐베르트 순서 추가
---

# 초기해 구축 25개 알고리즘

`solver-core`의 `solve` 패키지에 있는 초기해 구축 방법 25개를 **배경 지식 없이 읽을 수 있게**
풀어 쓴 노트다. **규칙의 원본은 [stage-04-initial-solution-heuristics.md](../implementation/stage-04-initial-solution-heuristics.md) §5**이고,
이 문서와 어긋나면 그쪽이 이긴다. 여기서는 "무엇을 노리고 만든 방법인가 / 어떻게 굴러가는가"만 다룬다.

## 0. 이 문서에서 쓰는 말

| 말 | 뜻 |
|---|---|
| **주문** | 일감 한 건. 픽업과 배송이 짝인 주문은 **통째로 하나**로 다룬다 — 픽업만 따로 빼는 일은 없다 |
| **미배정 목록** | 어느 차에도 싣지 못한 주문 모음. 실패가 아니라 정상적인 결과의 일부다 (문서 용어로는 bank) |
| **끼워넣기 후보** | "이 주문을 이 차의 이 순서 자리에 끼우면 되는가"를 **실제로 계산해 본** 결과. 되는 것만 후보로 남는다 |
| **실제 계산** | 그 경로의 시간표를 처음부터 다시 계산해 시간창·무게·부피·정차 한도·근무시간을 전부 지키는지 보는 것. 어림값이 아니다 (문서 용어로는 전파·오라클) |
| **점수** | `[미배정 수, 쓴 차량 수, 총 거리(m), 총 운행시간(초)]` 네 칸. **앞칸부터 차례로 비교**해 작은 쪽이 이기고, 앞칸이 같을 때만 뒷칸을 본다 (문서 용어로는 사전식) |
| **후회값** | 지금 가장 싼 자리에 안 넣고 미뤘을 때 **나중에 얼마나 더 비싸지는가**. 큰 주문일수록 먼저 처리한다 (문서 용어로는 regret) |
| **씨앗** | 빈 경로를 열 때 맨 처음 싣는 주문. 어떤 주문을 씨앗으로 고르느냐가 경로 모양을 좌우한다 (seed) |
| **구역** | 입력이 주문마다 붙여 준 지역 코드(ZONE_19 등). 한 경로는 구체 구역 1종만 담을 수 있다 |
| **건너뜀** | 그 방법이 이 문제 구조에서 성립하지 않으면 아예 돌리지 않는다. 실패가 아니다 (문서 용어로는 기권) |

25개는 **전부 난수를 쓰지 않는다.** 같은 입력이면 언제나 같은 답이 나온다. 25개를 모두 돌린 뒤
**정식 평가 점수로 가장 좋은 하나**를 골라 ALNS에 넘긴다.

---

## 1. 실물 데이터 실측 결과 (H25 편입 전 · 24개 전부)

`data/win_poc_case_floor.json` — 주문 452건 · 차량 31대 · 차당 정차 28회 한도 · 단일 차고.
**2026-09-04 단일 실행**(`InitialSolutionBuilder.build` 1회, 기본 프로필). 24개 전부 실행됐다(건너뛴 것 0개).
H25는 이 실행 뒤에 편입돼 이 표에 없다 — H25의 실물 수치는 [stage-04-h25 §8](../implementation/stage-04-h25-zone-quota-exchange.md)에 있다.
\* H23·H24의 소요는 존 배정 DP 개정([scaling](../implementation/stage-04-zone-quota-allocation-scaling.md))
뒤 재실측값이다(개정 전 502·786 ms). 점수 네 축은 개정 전후가 완전히 같다 — heuristics §8 T52가 고정한다.

| 순위 | # | id | 미배정 | 차량 | 거리(m) | 운행시간(s) | 소요 |
|---:|---:|---|---:|---:|---:|---:|---:|
| **1** | **H23** | **`zone-quota-balanced-fill`** | **0** | 31 | 4,198,408 | 1,002,069 | 550 ms* |
| 2 | H24 | `zone-quota-subset-fill` | 8 | 31 | 3,942,905 | 974,992 | 836 ms* |
| 3 | H3 | `vehicle-zone-fill` | 15 | 31 | 4,515,433 | 1,003,093 | 137 ms |
| 4 | H22 | `squeaky-wheel-sequential` | 47 | 31 | 4,666,316 | 967,429 | 399 ms |
| 5 | H14 | `vehicle-fill-remaining-regret` | 56 | 31 | 4,216,318 | 917,423 | 519 ms |
| 6 | H2 | `urgency-regret3` | 60 | 31 | 3,924,184 | 941,844 | 644 ms |
| 7 | H4 | `deadline-sequential` | 65 | 31 | 3,604,197 | 935,716 | 77 ms |
| 8 | H12 | `route-bidding` | 70 | 31 | 3,767,908 | 902,723 | 497 ms |
| 9 | H16 | `constrained-path-extension` | 78 | 31 | 2,582,030 | 877,665 | 45 ms |
| 10 | H13 | `slack-preserving-sequential` | 85 | 31 | 4,712,619 | 992,868 | 80 ms |
| 11 | H1 | `scarcity-regret2` | 86 | 31 | 2,750,454 | 915,946 | 830 ms |
| 12 | H11 | `i1-savings-sequential` | 87 | 30 | 3,475,573 | 882,098 | 558 ms |
| 13 | H9 | `feasible-routes-regret-m` | 95 | 31 | 4,353,137 | 924,321 | 552 ms |
| 14 | H15 | `weakest-fit-decreasing` | 103 | 31 | 2,306,187 | 872,664 | 89 ms |
| 15 | H6 | `zone-first-ffd` | 120 | 29 | 4,620,963 | 896,661 | 43 ms |
| 16 | H21 | `spatiotemporal-cluster` | 124 | 31 | 2,799,861 | 862,311 | 7,316 ms |
| 17 | H17 | `farthest-seed-global-cheapest` | 131 | 31 | 3,771,961 | 854,473 | 415 ms |
| 18 | H10 | `chain-seed-regret-m` | 146 | 31 | 4,098,773 | 869,722 | 393 ms |
| 19 | H19 | `nearest-neighbor-split` | 146 | 27 | 4,117,051 | 764,837 | 182 ms |
| 20 | H18 | `hilbert-split` | 165 | 30 | 5,353,989 | 840,954 | 175 ms |
| 21 | H5 | `farthest-seed-sequential` | 185 | 28 | 5,584,795 | 811,375 | 375 ms |
| 22 | H20 | `gap-sweep-bidirectional` | 238 | 31 | 1,695,237 | 783,315 | 13 ms |
| 23 | H8 | `sweep-next-fit` | 298 | 28 | 3,973,973 | 686,074 | 6 ms |
| 24 | H7 | `savings-merge` | 421 | 16 | 1,711,069 | 353,004 | 7 ms |

포트폴리오 24개 총 소요 **14.7초** (그중 H21 하나가 7.3초 = 절반).

**읽는 법 — 거리가 짧다고 좋은 게 아니다.** 순위는 첫 칸(미배정)부터 본다. H7이 거리 171만 m로
짧아 보이는 건 452건 중 **421건을 못 실어서** 남은 31건만 돌기 때문이다. 같은 이유로 H20·H15·H1의
짧은 거리도 품질 신호가 아니다. **차량 칸도 마찬가지다** — H19·H5·H8의 27~28대는 차를 아낀 게 아니라
남긴 주문이 많아서다. 미배정이 0인 H23만이 Win 엔진 결과(452건 전량·31대)와 같은 축에 있다.

> 이 표는 **한 번의 실행 기록**이고 T번호 테스트로 고정돼 있지 않다 (survey §2.5의 수치와 같은 성격).
> 회귀로 고정된 것은 T44 하나 — H23이 실물에서 전량 배정하고 H3보다 좋다는 사실이다.
> 재현 확인: H3(15/31)·H23(0/31)·H24(8/31)와 거리·운행시간이 survey §2.5의 2026-09-02 기록과 정확히 일치한다.

---

## 2. 알고리즘 25개

우선순위 순서다. 앞선 번호일수록 먼저 실행되고, 점수가 완전히 같으면 앞 번호가 이긴다.

### H1 `scarcity-regret2` — 갈 데 없는 주문 먼저

**목적.** "지금 안 넣으면 영영 못 넣을 주문"을 먼저 처리한다. 위험을 두 가지로 잰다 —
첫째 **실을 수 있는 차가 몇 대뿐인가**(적을수록 위험), 둘째 후회값.

**구현.** 남은 주문 전부에 대해 끼워넣기 후보를 계산해 가장 싼 자리와 두 번째로 싼 자리를 찾는다.
후회값 = (둘째 비용 − 첫째 비용). 후보가 딱 하나뿐이면 "지금 아니면 끝"이므로 최우선으로 끌어올린다.
그다음 `(실을 수 있는 차 수 적은 순 → 후회값 큰 순 → 주문번호 순)`으로 하나를 골라 가장 싼 자리에 넣고,
이걸 주문이 다 떨어질 때까지 되풀이한다. 어디에도 못 넣는 주문은 미배정으로 뺀다.

### H2 `urgency-regret3` — 시간이 촉박한 주문 먼저

**목적.** H1과 같은 방식이되 **급한 정도의 정의를 "받아 주는 시간대가 얼마나 좁은가"로** 바꿨다.
같은 계열이라도 기준이 다르면 다른 답이 나온다.

**구현.** 문제 전체 주문의 시간창 폭을 줄 세워 **4등급**으로 뭉갠다(0~3등급). 폭을 그대로 쓰면
등급이 다 달라져 후회값이 무의미해지므로 일부러 뭉갠 것이다. 그다음 각 주문의 후보 상위 3개 비용으로
후회값을 계산하고(3순위까지 보므로 H1보다 넓게 본다), `(급한 등급 → 후보 수 적은 순 → 후회값 큰 순)`으로
하나씩 넣는다.

### H3 `vehicle-zone-fill` — 차 한 대에 구역 하나를 통째로

**목적.** **쓰는 차량 대수를 직접 줄이는 유일한 방법.** 차 한 대를 잡으면 그 차에 가장 잘 맞는
구역 하나를 통째로 실어 꽉 채워 보내고 다음 차로 넘어간다.

**구현.** 까다로운 차(실을 수 있는 주문이 적은 차)부터 차례로 잡는다. 남은 주문을 구역별로 묶고,
구역마다 **가짜로 한 번 실어 본다** — 전원 다 실리는지, 적재율이 얼마인지만 보고 결과는 버린다.
"전원 성공하면서 적재율 85% 넘는" 구역이 있으면 그중 적재율이 가장 높은 구역을, 없으면 그냥 적재율이
가장 높은 구역을 **그때 진짜로 확정**한다. 아무 구역도 못 받은 차는 아예 쓰지 않는다.

### H4 `deadline-sequential` — 마감 임박 순 (기준선)

**목적.** 가장 단순한 방법. **다른 방법이 이보다 못하면 그 방법에 문제가 있다**는 판정 기준선 역할이다.

**구현.** 모든 주문을 마감 시각(받아 주는 마지막 시각)이 이른 순으로 한 줄로 세운 뒤, 앞에서부터 하나씩
그 시점에 가장 싼 자리에 넣는다. 되돌아가거나 다시 계산하는 일이 전혀 없어 25개 중 가장 빠른 축이다
(77 ms). 어디에도 안 들어가면 미배정.

### H5 `farthest-seed-sequential` — 먼 곳부터 경로 하나씩 완성

**목적.** H4가 모든 경로를 동시에 키우는 것과 반대로, **경로를 하나씩 끝내고 다음으로 넘어간다.**
차고에서 가장 먼 주문부터 여는 이유는, 먼 주문이 마지막까지 남으면 그것만을 위한 차를 한 대 더
열어야 하기 때문이다.

**구현.** 남은 주문 중 차고에서 가장 먼 것을 씨앗으로 새 경로를 연다. 그 경로에 더 못 넣을 때까지
가장 싼 것부터 계속 채운 뒤 그 경로를 확정하고, 다시 가장 먼 주문으로 새 경로를 연다.
차고가 2개 이상이면 "그 차고"가 정해지지 않으므로 건너뛴다.

### H6 `zone-first-ffd` — 먼저 나누고, 나중에 순서 정하기

**목적.** **누가 같은 차에 실릴지를 용량만 보고 먼저 정하고**, 그다음 각 묶음 안에서만 순서를 정한다.
두 걱정을 한꺼번에 하지 않아 묶음 모양이 H1·H2와 확연히 달라진다.

**구현.** 1단계 — 주문을 구역별로 묶고, 각 구역 안에서 큰 주문부터 "들어가는 첫 차"에 담는다
(고전적인 짐싸기 방식). 이 단계는 **시간창을 보지 않는다.** 2단계 — 그렇게 묶인 각 그룹 안에서만
H1의 후회값 방식으로 방문 순서를 정한다. 용량은 맞는데 시간이 안 맞아 못 들어가는 주문은 미배정이 된다
(그래서 실측 120건).

### H7 `savings-merge` — 합쳐서 이득 보는 순서로 잇기 (Clarke-Wright)

**목적.** 모든 주문을 일단 **각자 혼자 도는 경로**로 두고, 둘을 이어 붙였을 때 아끼는 거리가 큰 순서로
합쳐 나간다. 배차 문제에서 가장 오래된 고전 기법이다.

**구현.** 절감액 = (차고→A→차고) + (차고→B→차고) − (A→B로 바로 이음). 이 값이 큰 쌍부터,
A가 자기 경로의 꼬리이고 B가 다른 경로의 머리일 때만 잇는다. 합칠 때마다 **그 짐이 들어가는 가장 작은
차**로 갈아태워 큰 차를 아낀다. 픽업·배송 짝 주문이 있거나 차고가 2개 이상이면 건너뛴다.
실측 421건 미배정 — 이 데이터의 정차 한도·구역 규칙과 잘 맞지 않는다.

### H8 `sweep-next-fit` — 부채꼴로 훑으며 차 채우기

**목적.** 차고를 중심으로 **시곗바늘처럼 각도를 훑으며** 만나는 순서대로 차를 채운다. 가장 싸고
(6 ms) 공간적으로 자연스러운 경로 모양이 나온다.

**구현.** 차고 기준 각도로 모든 주문을 정렬한다(좌표는 **순서를 정하는 데만** 쓰고 거리는 반드시
이동표에서 읽는다). 차 한 대를 열어 각도 순으로 담다가 무게나 부피가 넘치면 **앞차로 되돌아가지 않고
바로 다음 차로 넘어간다** — 그래야 각도의 연속성이 유지된다. 배송 전용 주문만 있고 차고가 하나일 때만
쓸 수 있다.

### H9 `feasible-routes-regret-m` — "지금 실제로 들어갈 수 있는 경로 수"로 급한 정도 재기

**목적.** H1의 "실을 수 있는 차 수"는 처음부터 끝까지 안 변하는 값이다. 이걸 **지금 이 순간 실제로
들어갈 자리가 있는 경로가 몇 개인가**로 바꿨다. 경로가 차오르면 이 값이 줄어들어 그 주문이 자동으로
앞으로 당겨진다.

**구현.** 매번 남은 주문 전부의 후보를 경로별로 묶어 "들어갈 수 있는 경로 수"를 세고, 후회값도
2순위까지가 아니라 **모든 경로의 최선값을 다 합쳐서** 계산한다(그래서 regret-m). 그다음
`(들어갈 수 있는 경로 수 적은 순 → 후회값 큰 순)`으로 넣는다.

### H10 `chain-seed-regret-m` — 이어 돌 수 없는 주문만 씨앗으로

**목적.** **필요한 경로 수의 하한을 처음에 못 박는다.** 시간상 절대 한 차로 이어 돌 수 없는 주문들은
어차피 각각 다른 차가 필요하므로, 그것들만 미리 씨앗으로 심어 놓고 시작한다.

**구현.** 주문을 가장 이른 시작 시각 순으로 훑으며, 직전 씨앗을 끝낸 뒤 **가장 느린 차 속도로 이동해도**
제시간에 닿을 수 있으면 "이어 붙일 수 있다"고 보고 씨앗에서 뺀다. 못 잇는 것만 씨앗으로 남아 각각
새 경로를 연다. 나머지 주문은 H9와 같은 방식으로 채우되, **새 경로는 기존 경로에 도저히 못 들어갈
때만** 연다.

### H11 `i1-savings-sequential` — 혼자 두면 제일 비싼 주문부터 흡수 (Solomon I1)

**목적.** H5처럼 경로를 하나씩 완성하되, 다음에 넣을 주문을 **"이 주문을 혼자 돌게 하면 얼마나
비싼가"** 로 고른다. 혼자 두면 손해가 큰 먼 주문을 먼저 흡수하는 효과가 있다.

**구현.** 마감이 가장 이른 주문을 씨앗으로 경로를 연다. 그다음 후보마다
절감 = (그 주문 혼자 왕복하는 거리) − (지금 이 경로에 끼울 때 늘어나는 거리)를 계산해 절감이 가장 큰
것을 넣는다. 두 값 모두 단위가 미터라 그냥 뺄셈이다 — 서로 다른 단위를 가중치로 섞지 않는다.
더 못 넣으면 경로를 확정하고 새 씨앗으로 넘어간다.

### H12 `route-bidding` — 경로가 주문을 고르는 경매

**목적.** 다른 방법들은 대개 "주문이 갈 경로를 고른다". **이 방법만 방향이 반대다** — 주문들이
입찰하면 **경로가 그중 하나를 고른다.** 여러 경로가 한 라운드에 동시에 자라므로 먼저 열린 경로가
좋은 주문을 독식하지 못한다.

**구현.** 한 라운드에서 모든 주문이 각자 가장 싼 경로에 입찰한다. 그러면 각 경로는 자기에게 들어온
입찰 중 **가장 싼 것 딱 1건만** 받아들여 싣는다. 서로 다른 경로의 수락은 충돌하지 않으므로 동시에
반영된다. 한 라운드에 아무도 수락되지 않으면 종료한다.

### H13 `slack-preserving-sequential` — 시간 여유를 덜 빼앗는 자리로

**목적.** 지금 당장 싼 자리가 **다음 주문의 자리를 없애 버리는 것**을 막는다. 이 저장소의 시간 규칙상
근무창 끝을 넘기면 다음 창까지 통째로 밀리는 큰 지연이 생기는데, 그걸 정면으로 겨눈다.

**구현.** 넣는 순서는 H4와 똑같다(마감 임박 순). **자리를 고르는 기준만 바꿨다** — 거리가 아니라
"이 삽입 때문에 그 경로의 **기존** 방문들이 잃는 시간 여유의 합"이 가장 작은 자리를 고른다.
새로 넣는 방문 자신의 여유는 세지 않는다. 여러 기준을 하나로 섞지 않고 1순위 기준만 교체한 형태다.

### H14 `vehicle-fill-remaining-regret` — 큰 차부터 채우되, 뒤차에 맡길 건 맡긴다

**목적.** 차를 큰 것부터 하나씩 채운다. 다만 무작정 싼 것부터 담지 않고 **"이건 뒤에 오는 차가
더 잘 처리할 수 있다"** 싶으면 미룬다. H3와 같은 차량 중심이지만 구역 단위가 아니라 주문 단위다.

**구현.** 정차 한도·적재량이 큰 차부터 잡는다. 후보 주문마다
(지금 이 차에 넣는 비용) − (아직 안 쓴 뒤 차량들 중 이 주문을 혼자 실어 나르는 최선 비용)을 계산해
그 차이가 작은 것부터 싣는다. **뒤에 실을 수 있는 차가 하나도 없는 주문은 최우선**으로 끌어올린다.
더 못 넣으면 그 차를 확정하고 다음 차로 넘어간다.

### H15 `weakest-fit-decreasing` — 센 차를 아껴 둔다

**목적.** 주문은 **어려운 것부터**, 차는 **약한 것부터** 매칭한다. 큰 차·긴 근무시간을 가진 센 차를
쉬운 주문에 낭비하지 않고 어려운 주문 몫으로 남겨 두는 것이 목적이다.

**구현.** 어려운 순서 = `(실을 수 있는 차가 적은 순 → 시간창이 좁은 순 → 무거운 순)`.
약한 순서 = `(적재량 작은 순 → 근무시간 짧은 순)`. 어려운 주문부터 차례로, ① 이미 열린 경로를 약한 차
순으로 훑어 처음 들어가는 곳에 넣고 ② 없으면 아직 안 쓴 차를 약한 순으로 훑어 새 경로를 연다.
H6가 큰 차부터 여는 것과 정확히 반대 방향이다.

### H16 `constrained-path-extension` — 경로 끝에 계속 이어 붙이기

**목적.** 25개 중 **유일하게 "중간에 끼워넣지" 않고 경로 끝에만 잇는다.** 중간 삽입을 안 하니
아주 싸고(45 ms), 경로 모양이 다른 방법들과 확연히 다르게 나온다.

**구현.** 차를 큰 것부터 잡고 빈 경로에서 시작한다. 다음에 이을 주문은
`(실을 수 있는 차가 적은 순 → 지금 위치에서 가까운 순)`으로 고르고, 그 순서대로 끝에 붙여 보면서
**처음 성공한 것을 확정**한다. 픽업·배송 짝 주문은 `[…, 픽업, 배송]`으로 통째로 붙어 "픽업만 붙은
상태"가 아예 생기지 않는다. 전부 실패하면 그 차를 닫고 다음 차로 넘어간다.

### H17 `farthest-seed-global-cheapest` — 씨앗을 심고, 매번 전체에서 제일 싼 것

**목적.** 후회값을 아예 쓰지 않고 **매 순간 모든 (주문, 자리) 조합 중 전역 최저 하나**만 넣는 방식.
가장 순수한 탐욕법이라 후회값 기반(H1·H2)과도, 고정 순서(H4)와도 다른 답이 나온다.

**구현.** 먼저 차량의 25%에 대해 "차고에서 가장 먼 주문"을 씨앗으로 심어 경로 뼈대를 만든다.
그다음에는 남은 주문 전부의 최선 후보를 모아 그중 **전체에서 가장 싼 것 하나**를 넣기를 반복한다.
25%라는 비율은 조정 가능한 상수다.

### H18 `hilbert-split` — 한 줄로 세운 뒤 잘라내기 (공간 곡선)

**목적.** 나머지 방법들이 "다음에 뭘 어디에"를 탐욕으로 정하는 반면, 이 방법은 **순서를 먼저 통째로
고정한 뒤 어디서 끊어 경로를 나눌지를 계산으로 최적하게** 정한다. 같은 순서라면 어떤 탐욕적 분할보다
나쁠 수 없다는 성질이 있다.

**구현.** 주문 위치를 **힐베르트 곡선**(가까운 지점끼리 붙어 있게 평면을 한 줄로 훑는 곡선) 순서로
세운다. 그다음 "앞에서 i번째까지 처리하고 차 k대를 썼을 때의 최선"을 표로 채워 나가며(동적 계획법)
`[미배정 수, 차량 수, 거리]` 기준 최선 절단 지점을 찾는다. 각 절단 후보는 실제 계산으로 검증한다.
좌표는 줄 세우기에만 쓰고 거리는 이동표에서 읽는다.

### H19 `nearest-neighbor-split` — 가까운 곳부터 이어 세운 뒤 잘라내기

**목적.** H18과 **자르는 방법은 완전히 같고 줄 세우는 방법만 다르다.** 좌표가 아예 필요 없어
좌표가 신뢰할 수 없는 입력에서도 쓸 수 있다.

**구현.** 차고에서 출발해 "지금 위치에서 이동표상 가장 가까운 주문"을 계속 이어 붙여 한 줄을 만든다.
그 줄을 H18과 같은 동적 계획법으로 자른다. 차고가 하나여야 출발점이 정해지므로, 2개 이상이면 건너뛴다.

### H20 `gap-sweep-bidirectional` — 빈 곳에서 시작해 양쪽으로 훑기

**목적.** H8의 부채꼴 훑기를 세 가지로 개선했다 — ① 모든 주문 유형 지원 ② 나눌 때 용량이 아니라
**실제 계산**으로 판정 ③ **시계·반시계 양방향을 다 만들어 좋은 쪽 채택**. 시작 각도를 주문이 하나도
없는 가장 넓은 빈 구간에 두어 부채꼴이 밀집 지역을 가르지 않게 한다.

**구현.** 각도상 이웃 간 간격이 가장 큰 곳을 0도로 삼는다. 시계 방향과 반시계 방향 각각에 대해,
차를 하나씩 열고 각도 순으로 담되 **그 차와 맞지 않는 주문은 건너뛰어 뒷차 몫으로 남긴다**(H8은 여기서
바로 다음 차로 넘어간다). 두 방향의 결과를 정식 평가해 좋은 쪽을 반환한다.

### H21 `spatiotemporal-cluster` — 거리에 시간 궁합을 섞어 묶기

**목적.** H6는 입력이 준 구역 코드로 나눈다. 이 방법은 **"거리도 가깝고 시간대도 서로 맞는가"를 한
숫자로 만들어** 그걸로 묶는다. 구역 코드가 없거나 구역이 시간대와 어긋난 입력에서 H6와 전혀 다른
묶음이 나온다.

**구현.** 두 주문 사이 점수 = 이동거리 × (시간상 이어 돌 여유가 적을수록·짐이 무거울수록 커지는 계수).
처음엔 주문 하나가 각각 하나의 묶음이고, **점수가 가장 낮은(가장 잘 맞는) 두 묶음을 계속 합쳐 나간다**
(합칠 수 있는 쌍이 없을 때까지). 합친 묶음마다 차 한 대를 배정하고 그 안에서 H6 2단계와 같은 방식으로
순서를 정한다. 묶음 계산 비용 때문에 25개 중 가장 느리다(7.3초 = 포트폴리오 전체의 절반).

### H22 `squeaky-wheel-sequential` — 못 실은 주문의 우선순위를 올려 다시 짜기

**목적.** H1의 "갈 데 없는 정도"는 처음부터 정해진 값이다. 이 방법은 **한 번 짜 본 결과에서 배운다** —
못 실린 주문의 우선순위를 올려 놓고 처음부터 다시 짜기를 반복한다. 난수를 쓰지 않고 규칙만으로
갱신하므로 결과는 여전히 결정적이다.

**구현.** 모든 주문의 우선순위를 0에서 시작해 최대 5라운드를 돈다. 매 라운드 `(우선순위 높은 순 →
마감 임박 순)`으로 H4처럼 순차 삽입하고 정식 평가한다. 그다음 **못 실린 주문에 +2**,
방문 수가 중앙값에 못 미치는 부실한 경로에 실린 주문에 +1을 준다. 우선순위가 직전 라운드와 완전히
같아지면 더 돌아도 같은 답이므로 즉시 끝낸다. 5라운드 중 가장 좋았던 답을 반환한다.

### H23 `zone-quota-balanced-fill` — 구역별 차종 배분을 먼저 전역 계산 ★ 실측 1위

**목적.** 실물 데이터를 실측해 보니 **부피 여유가 4.2%밖에 없고, 구역마다 들어갈 수 있는 차종이 거의
정해져 있었다.** 그러면 "어느 구역에 어떤 차종을 몇 대 줄 것인가"가 곧 미배정 건수를 결정한다.
H3는 차 한 대씩 순서대로 구역을 고르다 보니 구역이 조각나고 뒤에 남은 작은 차들이 반만 찼다(15건 미배정).
그 조합을 **한 대씩이 아니라 전체를 한꺼번에** 정하는 것이 이 방법이다.

**구현.** 1단계(H24와 공용) — 적재량·정차 한도·실을 수 있는 주문 집합이 같은 차들을 **차종**으로 묶고,
동적 계획법으로 "구역별 차종 대수" 배분을 전역 최적으로 정한다. 이때 **큰 차가 큰 차 금지 구역으로
가지 않도록** 부분집합마다 공급이 수요를 덮는지 누적 검사한다. 2단계 — 각 구역 안에서 주문을
`(실을 수 있는 차종이 적은 순 → 부피 큰 순)`으로 싣되, 차를 고르는 기준이 특이하다:
**"남은 정차 횟수를 평균 크기 주문으로 다 채웠을 때 부피가 얼마나 남는가"** — 이 값이 음수면 정차가
먼저 바닥나 부피를 버리게 되므로 뒤로 미루고, 양수 중 가장 작은 차가 가장 잘 맞는 차다. 마지막으로
못 실은 주문은 **이미 실린 더 작은 주문과 1:1로 맞바꿔** 보고, 그래도 남으면 다른 경로에 넣어 본다.

### H24 `zone-quota-subset-fill` — 같은 배분 위에서 "정확히 딱 맞는 조합" 찾기

**목적.** H23과 **1단계는 완전히 같고 2단계만 다르다.** 구역 안을 채울 때 하나씩 순서대로 담는 대신
**부피 합이 차 용량에 정확히 들어맞는 주문 조합을 계산으로 찾는다.** 실측에서 미배정은 8건으로 H23에
지지만 **거리는 6% 더 짧다** — 어느 쪽이 최종적으로 남을지는 Stage 8 실측이 정한다.

**구현.** 차마다 씨앗(차고에서 가장 먼 주문)을 잡고, 이동 시간이 가까운 순으로 후보 풀을 자른다
(부피만 보면 넓게 흩어진 주문을 한 차에 몰아 시간창을 어기기 때문이다). 그 풀에서
**부분집합 합 동적 계획법**으로 부피·무게·정차 수를 만족하는 조합을 찾는다. 그다음 그 조합을 실제로
실어 보고, **시간창 때문에 실패한 주문이 있으면 그것을 제외 목록에 넣고 다시 계산한다**(실패에서 배우는
되먹임). 반복할 때마다 제외가 늘거나 개수 상한이 줄어들어 반드시 끝난다.

### H25 `zone-quota-exchange-fill` — 구역끼리 차를 맞바꿔 배분을 고친다 (2026-09-04 편입)

**목적.** H3가 15건을 남긴 이유는 "채우는 솜씨"가 아니라 **"어느 구역에 어떤 차를 줄지 정하는 방식"**이었다.
차 한 대씩 그 자리에서 구역을 확정하니 구역이 조각나고, 마지막에 남은 작은 차 4대가 반만 찼다.
그러면 **반만 찬 차를 다른 구역의 차와 맞바꾸거나, 남는 구역의 차 한 대를 모자란 구역으로 옮기면**
어떻게 될까 — 24개 어디에도 그 단계가 없었다(H23의 1:1 교환은 **한 구역 안에서 주문 둘**을 맞바꾸는
것이라 한 층 아래다). H25는 그 층을 연다. **H23의 동적 계획법만큼 좋아지는지는 논리로 답할 수 없어서
만들어 재 본다** — 남길지 지울지는 실측이 정한다.

**구현.** 1단계 — 구역을 순서대로 돌며 "이 구역을 덮는 데 필요한 최소한의 차"를 큰 차부터 한 대씩
번갈아 넣어 채우고(그래야 특정 차종만 실을 수 있는 주문 묶음을 빠뜨리지 않는다), 다 덮이면 작은 차부터
빼며 군더더기를 깎는다. 2단계 — 구역 A의 차 한 대를 구역 B로 **옮기거나**(MOVE), A와 B의 차를
**맞바꿔**(SWAP) 본다. 배분의 좋고 나쁨은 H23의 동적 계획법과 **똑같은 잣대** —
`(못 싣는 양, 남는 빈 공간, 쓴 차 대수)`를 앞에서부터 비교 — 로 재고, 매 라운드 가장 좋아지는 한 수만
적용하며 더 나아질 수가 없으면 멈춘다(최대 50라운드). 배정하지 않은 차들을 모아 둔 "대기소"도 구역처럼
다뤄서 "남는 차를 돌려보내기"·"모자란 구역에 주기"가 같은 규칙으로 나온다. 3단계 —
**구역 안을 채우는 일은 H23의 2단계를 그대로 호출한다.** 그래야 H23과의 차이가 "동적 계획법이냐
탐욕+교환이냐" 하나로 좁혀진다.

---

## 3. 이 표에서 읽어야 할 것

- **구조를 맞춘 방법이 이긴다.** 상위 3개(H23·H24·H3)는 전부 **구역과 차종의 관계**를 정면으로 다룬다.
  범용 삽입 기법(H1·H9·H17)은 이 데이터에서 86~131건을 남긴다 — 이 문제의 병목이 "어디에 끼울까"가
  아니라 "어느 구역에 어떤 차를 줄까"이기 때문이다.
- **전역으로 정하는 것이 이긴다.** H3(한 대씩 순서대로 구역 선택) 15건 → H23(전체 배분을 한꺼번에 계산)
  0건. 방법의 성격은 같고 결정 범위만 넓혔는데 남은 15건이 사라졌다.
- **빠른 것이 나쁜 것은 아니다.** H4는 77 ms에 65건인데 830 ms를 쓰는 H1이 86건이다. 정교한 기법이
  항상 이기지 않는다 — 25개를 다 돌려 보고 고르는 이유가 이것이다.
- **25 → n 축소는 계약상 아직 미결이다.** 문서 §10 Q1이 "Stage 8 실측 후 사용자 결정"으로 열어 둔 항목이고,
  이 표가 그 판단의 입력 중 하나다. 어떤 기준으로 무엇을 남길지의 검토는 §4, 25개 밖의 방향은 §5.

---

## 4. 병렬 포트폴리오 — 25개 중 무엇을 남기는가 (2026-09-04 검토)

### 4.1 전제 — 왜 "실측 상위 n개"로 자르지 않는가

- **결정된 것.** 25개를 차례로 다 돌리는 대신 **4~8개를 병렬로 돌려 best 하나를 고른다**(더 적을수록 좋다).
  이 절은 그 후보를 고르는 근거이고, 계약 개정은 §4.4 끝에 적은 순서로 따로 한다.
- **§1 표는 fixture 하나의 결과다.** 이 데이터의 병목은 "구역 × 차급 × 부피 여유 4.2%"라 구역을 정면으로
  다루는 방법이 상위를 독식했다. 구역이 없는 입력, 시간창이 복잡한 입력, 무게·부피 제약이 제각각인
  입력에서 어느 방법이 이길지는 **이 표가 말해 주지 않는다.** 그래서 상위 n개를 그대로 남기면
  "이 fixture 전용 포트폴리오"가 된다 — H23 다음이 H24라고 H24를 남기는 식이다.
- **자르는 기준 = 입력 구조의 축마다 그 축을 정면으로 다루는 대표 1개.** 같은 축을 다루는 방법이 둘이면
  실측이 좋은 쪽 하나만 남긴다(survey §3의 "같은 축 하나만" 원칙을 포트폴리오에도 적용).
- **초기해의 임무는 점수 1·2번 칸(미배정·차량 수)이다.** 3·4번 칸(거리·운행시간)은 ALNS가 잘 줄이는 축이라
  초기해 단계에서 거리가 짧은 방법을 우대하지 않는다. H24의 "거리 6% 단축"이 H23 대신 남을 이유가 못 되는
  근거다.
- **ALNS가 매 반복 하는 일과 같은 종류의 초기해는 자리 값이 낮다.** ALNS의 repair는 greedy 삽입과 regret
  삽입이다([stage-04-alns](../implementation/stage-04-alns.md) §4). 그러므로 "주문을 줄 세워 가장 싼
  자리·후회값으로 끼워 넣는" 계열(H1·H9·H17)은 ALNS가 destroy 뒤마다 다시 만드는 구조와 같다.
  반대로 **ALNS가 만들어 내기 어려운 구조** — 전역 배분(H23), 순열 고정 뒤 최적 절단(H18), 차 한 대씩
  닫아 가는 채우기(H14), 실패에서 배워 재정렬(H22) — 가 병렬 슬롯을 차지할 가치가 있다.
- **어떤 입력에서도 돌아가는 방법을 우선한다.** 차고가 2개면 H5·H7·H8·H19·H20이 건너뛰고, 픽업·배송 짝
  주문이 있으면 H7·H8이 건너뛴다(계약 §4.4). 그런 방법이 슬롯을 차지하면 그 입력에서는 슬롯이 빈다.

### 4.2 축과 대표

| 축 | 어떤 입력에서 갈리는가 | 후보 | 남김 | 남긴 이유 · 뺀 이유 |
|---|---|---|---|---|
| ① 전역 배분 | 구역·차급 호환이 강하고 부피 여유가 적을 때 | H23 · H24 · H25 · H3 · H6 | **H23** | H24는 1단계가 H23과 같고 2단계만 다르다 — 미배정 0 vs 8이고 거리는 ALNS 몫. H3는 H23의 국소판(한 대씩 구역 선택 → 15건)이라 같은 축의 하위 호환. H6(120)은 시간창을 안 보는 1단계가 약점. **구역이 없어도 H23은 돈다** — `ZoneQuotaAllocation.zones`가 부재 구역을 `(none)` 하나로 묶어 "차종별 대수 배분 + 정차 예산 best-fit"으로 퇴화한다(건너뜀 없음 — 차종 조합이 커지면 DP가 근사로 내려갈 뿐 답은 낸다,
[stage-04-zone-quota-allocation-scaling](../implementation/stage-04-zone-quota-allocation-scaling.md)). H25는 같은 축에서 1단계만 다른(탐욕 + 구역 간 교환) 기법이라 **H23이 있는 한 슬롯을 나눠 갖지 않는다** — 잔류 여부는 [stage-04-h25 §8](../implementation/stage-04-h25-zone-quota-exchange.md) 결정 표대로. **다만 §4.3 (a) "구역 지운 변형"의 실측(2026-09-04, stage-04-h25 §8.1 ②)에서 순위가 뒤집힌다** — 구역이 없으면 H3 122 · H25 **0** · H23 0(차량 30)으로, H3가 이 축의 하위 호환이라는 판단은 구역이 **있을 때**의 이야기다 |
| ② 무가정 자기 교정 | 병목이 무엇인지 모를 때 — 구역·좌표·차고 수·주문 유형 전부 무관 | H22 · H4 | **H22** | H22의 1라운드는 **H4와 같은 실행**이다(우선순위 전부 0 → 마감순 → 가장 싼 자리, `DeadlineSequentialConstruction.construct` 공유). 5라운드 중 최선을 돌려주므로 H4보다 나쁠 수 없다. 못 실린 주문에 우선순위를 주는 규칙은 "무엇 때문에 못 실렸는가"를 묻지 않아 어떤 제약 구조에서도 같은 방식으로 작동한다. 실측 4위(47) |
| ③ 시간창 | 받는 시간대가 좁고 제각각이거나 하루에 두 창으로 갈릴 때 | H2 · H13 · H10 · H21 | **H2** | 시간창 폭 4등급 + 후회값 3순위 — 범용 삽입 중 최선(60). H13(85)은 같은 축의 다른 관점(자리를 "여유 손실"로 고름)이라 §4.4의 조건부 6번째. H10(146)은 "이어 돌 수 없는 주문만 씨앗"이 이 데이터에서 약했고 다른 데이터의 근거도 없다. H21은 7.3초(당시 24개 전체의 절반) |
| ④ 차량 단위 채우기 | 차종이 제각각이고 차량 수가 빠듯한데 **구역이 없을 때** | H14 · H15 · H3 | **H14** | 구역과 무관하게 차 한 대를 잡아 꽉 채우고 닫는다. "뒤차가 더 잘 실을 것"을 미루는 기준이 이종 fleet를 정면으로 겨눈다. 실측 5위(56). H15(103)는 약한 차부터 쓰는 반대 방향인데 이 데이터에서 크게 졌고 다른 근거가 없다. ALNS의 repair는 주문 하나씩 싼 자리에 넣는 방식이라 "차 한 대를 통째로 채워 대수를 줄이는" 구조는 초기해가 줘야 한다 |
| ⑤ 공간 순서 + 최적 절단 | **구역이 없고** 좌표가 있을 때 — 지리적 묶음을 방법이 스스로 찾아야 할 때 | H18 · H19 · H20 · H8 | **H18** | 25개 중 유일하게 "순서를 고정하고 어디서 자를지를 계산으로 최적"하며 차고 수와 무관해 건너뜀이 없다. 실물 165건은 구역 × 차급 규칙을 1차원 순서로 표현할 수 없어서라는 **가설**(§4.3 (a)에서 검증). H19는 같은 절단에 순열만 다르고 단일 차고 요구. H20(238)·H8(298)은 부채꼴 훑기라 단일 차고 요구 + 실측 하위 |

**제외 — 남은 20개의 사유.**

| 제외 | 사유 |
|---|---|
| H1 · H9 · H17 | ALNS repair(regret 삽입·greedy 삽입)와 같은 계열 — 슬롯 값이 낮다(§4.1). 실측도 86·95·131 |
| H24 · H25 · H3 · H6 | 축 ① — H23이 상위 호환 (H25는 [stage-04-h25 §8](../implementation/stage-04-h25-zone-quota-exchange.md) 결정 대기) |
| H4 | 축 ② — H22의 1라운드가 H4다. **Stage 8 실측표에는 baseline으로 계속 싣는다**(survey §5 ④) — 포트폴리오 슬롯을 차지하지 않을 뿐 |
| H13 · H10 · H21 | 축 ③ — H2가 대표. H13은 조건부 6번째(§4.4) |
| H15 | 축 ④ — H14가 대표 |
| H19 · H20 · H8 | 축 ⑤ — H18이 대표 + 단일 차고 요구 |
| H5 · H11 | "경로 하나씩 완성" 계열 — 실측 185·87. 단일 차고 요구(H5) |
| H7 | 픽업·배송 짝이 있으면 건너뜀 + 실측 421 |
| H12 | 경매 방식 — 실측 70(8위)으로 나쁘지 않지만 새로 여는 축이 "누가 고르는가"뿐이라 다른 입력에서 갈릴 근거가 없다 |
| H16 | 끝에 잇기 — 45 ms로 싸지만 병렬에서는 시간이 아니라 **슬롯이 비용**이다. 실측 78(9위) |

### 4.3 이 선정이 아직 검증하지 못한 것 — Stage 8에 넘기는 실측

실측이 fixture 하나뿐이라 §4.2의 축 ④·⑤는 **논리로 고른 것이지 실측으로 고른 것이 아니다.** 아래 세 실행이
그 논리를 실측으로 바꾼다. 셋 다 기존 fixture의 변형이라 새 데이터가 필요 없다.

- **(a) 구역을 지운 변형** — `data/win_poc_case_floor.json`에서 `zoneId`를 전부 빼고(구역 1종 규칙이 적용되지
  않음) 25개 실행. **부분 실측 완료 (2026-09-04, stage-04-h25 §8.1 ②)** — H23은 퇴화해도 미배정 0(차량 30)으로 상위라는 예측이 맞았다. H18·H14는 아직 안 쟀다. 예측: H23은 퇴화해도 상위, H18·H14가 지금보다 크게 오른다. 예측이 틀리면 축 ④·⑤의
  대표를 바꾼다.
- **(b) 시간창을 조인 변형** — 05:45–13:30(169건)을 두 창(오전/오후)으로 쪼개거나 폭을 절반으로. H2 vs H13이
  여기서 갈린다.
- **(c) 벽시계 시간** — 5개 병렬은 가장 느린 것(H23 550 ms)이 전체 시간이다. 24개 순차 14.7초(H25 편입 전)에서
  **약 0.6초**로 준다. ALNS 예산(`AlnsConfig`)과의 비율은 Stage 8 표의 열이다.

### 4.4 결론 — 5개, 조건부 1개

**남기는 후보 5개: H23 · H22 · H2 · H14 · H18.** 점수가 완전히 같을 때의 우선순위는 계약(§2 첫 줄)대로
번호 순(H2 → H14 → H18 → H22 → H23)을 유지한다 — 실물 fixture에서는 동률이 없다.

- **조건부 6번째: H13** — §4.3 (b)에서 H2가 약하면 넣는다. 7·8번째는 비워 둔다 — 늘리는 근거는 실측뿐이다.
- **이 절은 계약이 아니다.** 확정하려면 다음 순서로 문서를 먼저 개정한다 —
  [stage-04-heuristics](../implementation/stage-04-initial-solution-heuristics.md) §10 Q1 해소("25 → n"을 "25 → 5"로,
  §4.4 기권 절의 "25개 전부 실행" 문구 정정) →
  [survey §5](../implementation/stage-04-initial-solution-heuristics-survey.md) 프로토콜의 자르는 순서에 "축 대표" 기준 반영 →
  [Stage Extra E4](../implementation/stage-extra-deferred-features.md) 트리거 발동(병렬 실행기·스레드 수 = Q2) →
  `InitialSolutionBuilder` 기본 목록과 T25·T44 조정. **그 전까지 코드는 25개 전부 실행한다.**
- **후속 설계 2건 (2026-09-04).** H23이 차종 조합 수 Π > 65,536에서 기권하던 문제는
  [stage-04-zone-quota-allocation-scaling](../implementation/stage-04-zone-quota-allocation-scaling.md)이
  기권 자체를 없애는 쪽으로(성분 분해·풍부 차종 제외 → 희소 상태 → 총량 폭 제한), "greedy + 구역 간 교환"이
  H3보다 얼마나 나은지는 [stage-04-h25](../implementation/stage-04-h25-zone-quota-exchange.md)가 H25를 H3 옆에
  두고 재는 쪽으로 다룬다. 구현 순서는 scaling 먼저, H25 다음. **scaling은 2026-09-04에
  계약 반영·구현됐다 — H23·H24는 이제 어떤 입력에서도 기권하지 않으므로 대체 기법이 필요 없다.**
  H25는 2026-09-04에 계약 반영·구현됐다 — **H23 기권 입력에서는 [stage-04-h25 §8](../implementation/stage-04-h25-zone-quota-exchange.md)
  결과가 슬롯을 정한다**(기권이 사라진 지금 그 자리는 비어 있고, H25의 잔류 여부는 그 §8 결정 표가 정한다).

---

## 5. 25개 밖의 방향 — 다른 갈래가 있는가

survey §4가 이미 기각한 것(난수 multi-start·MIP·학습 기반·route elimination → ALNS 연산자)과 보류한 것
(Ioannou impact·2-petal·Dondo hyper-node·backhaul 매칭·다중 차고 urgency·VROOM λ 격자)은 다시 세지 않는다.
여기서는 **25개가 갖고 있지 않은 것**만 찾는다.

25개를 뜯어 보면 전부 두 결정의 조합이다 — ① **누구를 어느 차에**(분할) ② **차 안에서 어떤 순서로**(채우기).
그리고 한 방법은 그 둘의 **고정된 짝 하나**다. 그 관찰에서 세 방향이 나오고, 네 번째는 설계 결정이 먼저다.

### 5.1 존이 없을 때 존을 만든다 — 파생 구역 키 (권고 1순위)

H23의 힘은 구역 배정 DP인데 구역은 **입력이 준 `zoneId`에서만** 온다. 구역이 없는 입력에서 H23은 단일 구역으로
퇴화해 그 힘을 잃는다(§4.2 ①). 구역 부재 시 **결정적으로 파생한 키**를 대신 쓰면 그 힘이 돌아온다 —
(a) 차고 기준 각도 부채꼴 k등분(H8·H20의 훑기 순서), (b) 힐베르트 순서 k분위(H18의 순열), (c) 시간창 밴드
(오전/오후) × (a)나 (b). 파생 구역은 **construction 안에서만 쓰는 묶음**이고 hard 규칙(구역 1종)이 아니므로
leftover pass에서 묶음을 넘어 다닐 수 있다. 비용은 `ZoneQuotaAllocation.zones`의 키 함수 하나 — 새 방법이라기보다
**H23의 입력 일반화 변종 1개**다. 단 k는 파라미터라 Stage 8 실험 항목이 된다. §4.3 (a) 실측이 "구역 없으면
H23이 무너진다"를 보이면 그때 만든다.

### 5.2 분할 × 채우기의 부품화 (권고 2순위 — 실측이 빈 축을 보일 때만)

H23의 1단계(구역 → 차종 대수 DP)는 H23·H24의 채우기와만 짝지어져 있다. 그 위에서 H2의 채우기(급한 등급 +
후회값)나 H13(여유 손실)을 돌리면 "구역 구조 + 복잡한 시간창" 입력을 다루는 방법이 생기고, 반대로 H18의
절단 DP를 H21의 시공간 묶음 위에서 돌릴 수도 있다. 부품이 전부 있어 조립만 하면 되지만, **격자를 만들지는
않는다** — 조합은 폭발하고 그중 대부분은 갈리지 않는다(survey §3의 "같은 축 하나만" 원칙). §4.3 실측이 어느 축이 비어 있는지
보여 주면 **그 짝 하나만** 만든다.

### 5.3 결정적 폭 제한 탐색 — beam (예산 확인 후)

25개는 전부 **한 줄 탐욕**이다(H22만 5라운드, H25만 교환 스캔, H18·H23의 DP는 한 단계만 최적). 순차 삽입에서 부분해를
B개(2~4) 유지하고 매 단계 정식 평가 사전식으로 B개만 남기면, 초반에 잘못 연 경로를 되돌리는 수단이
ALNS 말고도 생긴다. 난수가 없어 결정적이다. 비용은 ×B라 초기해가 ALNS 예산을 잠식할 수 있다 —
§4.3 (c)에서 5개 병렬의 벽시계 시간이 ALNS 예산 대비 충분히 작음을 본 뒤에만 후보가 된다.

### 5.4 여러 초기해의 경로를 섞기 — route pool 재조합 (설계 결정이 먼저)

병렬 5개가 만든 경로들을 한 풀에 모아, 겹치지 않는 경로를 골라 미배정이 가장 적은 조합을 만든다 —
각 해가 잘 푼 구역·지역을 합치는 것이라 잠재력은 가장 크다. MIP set partitioning은 범위 밖이고
(master §4·§6, survey F4) 결정적 greedy 선택은 가능하지만, **Domain §9.3 "best 1개만 넘긴다"와 정면으로
충돌**한다(heuristics §9 "top-K 다양해 보관 안 함"). 문서 개정 없이는 못 가는 방향이라 여기 적어만 둔다.

### 5.5 하지 않을 것 (재확인)

- 난수 multi-start·GRASP — F1(결정성). 학습 기반 — F4.
- **입력을 진단해 실행할 방법을 고르는 meta-selection** — 5개면 그냥 다 돌리는 것이 단순하고 결정적이다.
  진단 수치(구역 유무·부피 여유율·시간창 폭 분포)는 Stage 8 실측표의 **열**로만 쓴다.

**지금 만들 것은 없다.** §4.3 (a)·(b) 실측이 먼저고, 그 결과가 5.1을 만들지, 5.2의 어느 짝을 만들지 정한다.

---

## 6. 포트폴리오 4개를 예시로 풀어 쓰기 — H22 · H2 · H14 · H18

§4.2 표의 "무가정 자기 교정", "후회값", "차량 단위", "공간 순서 + 최적 절단" 같은 말을 배경 지식 없이 읽을 수 있게
**작은 예시**로 다시 쓴 것이다. §2의 항목보다 길고, 규칙의 원본은 여전히 계약 §5다. H23은
[zone-quota-balanced-fill 노트](zone-quota-balanced-fill-construction.md)가 따로 다룬다.

### 6.0 먼저 — 네 방법이 공통으로 쓰는 "비용"

주문을 어느 자리에 끼울지 고를 때의 **비용**은 배차안 점수가 아니라 "끼웠을 때 **늘어나는 양**"이고, 세 칸을
앞에서부터 차례로 비교한다(`InsertionSearch.Candidate.byCost`).

| 순서 | 칸 | 뜻 |
|---|---|---|
| 1 | 새 차를 여는가 | 이미 도는 차의 자리가 빈 차를 새로 여는 자리보다 항상 앞선다 — 차량 수가 점수 2번 칸이라서 |
| 2 | Δ거리 (m) | 그 자리에 끼우면 그 경로의 총 주행거리가 얼마나 느는가. 이동표에서 읽는다 |
| 3 | Δ운행시간 (초) | 거리까지 같을 때만 |

예: 경로가 `차고 → P → Q → 차고`이고 P·Q 사이에 X를 끼우면 Δ거리 = (P→X) + (X→Q) − (P→Q). 후회값 계산에는
2번 칸(Δ거리)만 쓴다 — 단위가 미터로 같은 것끼리만 뺀다.

**차량 크기는 비용에도 점수에도 없다.** 1톤이든 5톤이든 "한 대"다. 크기는 제약(부피·무게 한도·차급 호환)과
몇몇 방법의 순서 규칙(H14는 큰 차부터, H15는 작은 차부터)으로만 작용한다. 확정된 설계다 — 점수 네 칸은 Domain이
고정했고, 차량 고정비 항은 survey §4에서 "profile이 차량 비용 축을 갖게 되면 재검토"로 기각돼 있다. 넣으려면
profile의 점수 축과 `byCost` 양쪽에 같은 축을 더해야 하고, 그것은 Stage 8 이후 별건이다.

### 6.1 H22 — "못 실은 주문을 다음 판에 먼저 실어 본다"

**용어.** "무가정" = 구역이 있다·좌표가 있다·차고가 하나다 같은 **전제를 하나도 깔지 않는다**. 그래서 어떤 입력에도
똑같이 돌아간다. "자기 교정" = 한 번 짜 본 결과를 보고 **자기 순서를 고쳐서 다시 짠다**.

**예시.** 주문 5건(A~E), 차 2대.

- **1판** — 마감이 이른 순 `A, B, C, D, E`로 줄 세우고 앞에서부터 가장 싼 자리에 끼운다. A~D는 들어갔는데 E는
  부피가 커서 자리가 없다 → 미배정. 다른 방법이라면 여기서 끝이다.
- **점수 매기기** — 못 실린 E에 우선순위 **+2**. 방문 수가 중앙값에 못 미치는 헐렁한 경로에 실린 주문에는 **+1**
  (그 경로를 더 꽉 채워 보라는 뜻).
- **2판** — `우선순위 높은 순 → 마감순`으로 다시 세우면 `E, A, B, C, D`. E가 맨 앞이니 빈 차에 제일 먼저 들어간다.
- **반복** — 최대 5판. 우선순위가 직전 판과 같아지면(못 실린 주문이 없거나 같은 주문이 같이 밀리면) 더 돌아도
  같은 답이라 즉시 멈춘다. 5판 중 **점수가 가장 좋았던 판**을 낸다.

"삐걱대는 바퀴(squeaky wheel)에 기름을 친다" — 문제를 일으킨 놈부터 챙긴다는 속담이 이름의 출처다.

**왜 남겼나.** ① 1판의 순서와 끼우는 규칙이 H4와 **글자 그대로 같은 코드**라(`DeadlineSequentialConstruction.construct`
공유) 5판 중 최선을 고르는 H22는 H4보다 나쁠 수 없다 — H4를 따로 넣을 필요가 없어진다. ② 왜 못 실렸는지를 묻지
않는다 — 부피든 시간창이든 구역 규칙이든 "못 실린 놈을 앞으로"만 하므로 병목을 모르는 입력에서도 같은 방식으로
작동한다. ③ 실물 4위(47), 전제 없는 방법 중 1등.

**비용.** 판 수는 방문 수와 무관하게 **최대 5** — 코드의 `ROUNDS = 5`, 다른 "재량 상수"(H3의 85%, H17의 25%)와
같은 부류로 설정 파일이 아니라 상수이고 Stage 8 실험이 조정한다. 한 판은 H4 한 번 값이다: 주문 하나를 끼울 때
경로마다 (방문 수 + 1)곳의 자리를 시험하고, 자리마다 그 경로 시간표를 처음부터 다시 계산한다. 차 한 대가
20군데를 돌고 있으면 그 차에만 21자리 × 21방문 ≈ 440번의 방문 계산. 실물(452건·31대)에서 한 판 ≈ 300만 번
≈ 80 ms, 5판 399 ms. 이 제곱 비용은 끼워 넣기를 쓰는 방법 대부분이 똑같이 치르고, H22는 거기에 ×5가 붙는 것뿐이다.
5판을 실제로 다 도는지는 실물에서 확인돼 있지 않다(399 ≈ 77 × 5라 추정만).

**한계.** "앞으로 당기기"는 자리를 만들어 줄 뿐, 구역 × 차종 조합 같은 전역 구조는 못 찾는다 — 그건 H23의 몫이다.

코드 수준 해설은 [squeaky-wheel-sequential 노트](squeaky-wheel-sequential-construction.md).

코드 수준 해설은 [squeaky-wheel-sequential 노트](squeaky-wheel-sequential-construction.md).

### 6.2 H2 — "시간대가 좁은 주문부터, 미루면 손해가 큰 것부터"

**용어.** "시간창" = 그 주문을 받아 주는 시간대. 폭이 좁을수록 끼울 자리가 적다. "후회값" = 지금 제일 싼 자리에
안 넣고 미뤘다가 그 자리를 뺏기면 **얼마나 더 비싸지는가**. X의 자리가 100·900·950 m면 첫 자리를 놓치는 순간
800 m 손해(후회값 큼), Y가 100·110·120 m면 놓쳐도 별 손해 없음(작음) → X 먼저.

**예시.** 주문 6건의 시간창 폭이 30분(A)·1시간(B)·2시간(C)·4시간(D)·6시간(E)·8시간(F).

- **1단계 — 급한 등급.** 폭을 그대로 쓰지 않고 전체를 줄 세워 **4등급**으로 뭉갠다: A·B 0등급(가장 급함), C 1등급,
  D 2등급, E·F 3등급. 왜 뭉개나 — 폭을 그대로 쓰면 주문마다 값이 달라 "급한 순" 하나로 순서가 끝나 버리고,
  다음 기준인 후회값이 쓰일 틈이 없다. 같은 등급끼리만 후회값으로 겨루게 하려고 일부러 거칠게 만든 것이다.
- **2단계 — 매번 하나씩.** 남은 주문 전부의 "넣을 수 있는 자리 상위 3곳" 비용을 계산하고,
  `급한 등급 → 자리 수 적은 것 → 후회값 큰 것` 순으로 하나를 골라 가장 싼 자리에 넣는다. 방금 넣은 것이 자리를
  바꿔 놓았으니 남은 주문의 자리를 **다시 계산**하고 반복한다. 위 예시에서 F(8시간)는 어디든 들어가니 맨 나중.

**H1과의 차이.** 둘 다 "위험한 주문 먼저 + 후회값"인데 위험의 정의가 다르다 — H1은 "실을 수 있는 차가 몇 대"(차종
호환, 후회값 2순위까지), H2는 "시간대가 얼마나 좁은가"(후회값 3순위까지). 실물에서 H1 86 vs H2 60 — 이 데이터에서는
차종 호환보다 시간창이 더 정확한 위험 신호였다.

**왜 남겼나.** 주문마다 받는 시간이 제각각이고 영업시간 때문에 오전/오후로 갈리는 입력에서, H23(구역 중심)도
H14(차량 중심)도 시간을 1순위로 보지 않는다. H2는 시간을 1순위로 보는 방법 중 실물 최선이다. 같은 축의 H13은
"어느 주문을 먼저"가 아니라 "어느 **자리**에"를 시간 여유로 고르는 방법이라 §4.4의 조건부 6번째.

**한계.** 매번 남은 주문 전부를 다시 계산하므로 H4보다 훨씬 비싸다(644 ms vs 77 ms). 시간창이 전부 비슷한
입력(모두 "하루 종일")에서는 등급이 의미를 잃어 사실상 H1과 비슷한 답이 나온다.

코드 수준 해설은 [urgency-regret3 노트](urgency-regret3-construction.md).

코드 수준 해설은 [urgency-regret3 노트](urgency-regret3-construction.md).

### 6.3 H14 — "차 한 대를 잡고 꽉 채워 보내되, 뒤차가 더 잘 할 주문은 남겨 둔다"

**용어.** "이종 fleet" = 차가 제각각(1톤·2.5톤·5톤, 정차 한도·근무시간도 다름). "차량 단위 채우기" = H22·H2가
**주문**을 집어 "어느 차의 어느 자리에"를 고르는 것과 반대로, **차**를 집어 "이 차에 뭘 실을까"를 고르고 다 채우면
그 차를 **닫는다**. 닫힌 차는 다시 열지 않는다.

**예시.** 차 3대(5톤 V1, 2.5톤 V2, 1톤 V3), 주문 6건.

1. **차 순서** — 정차 한도·적재량 큰 차부터: V1 → V2 → V3.
2. **V1 채우기** — 남은 주문마다 두 숫자: ① 지금 V1에 넣는 비용(Δ거리) ② 뒤차(V2·V3) 중 이 주문을 **혼자**
   실어 나를 때의 최선 비용. ①−②가 작을수록(음수로 클수록) "지금 싣는 게 이득"이니 먼저 싣는다.

   | 주문 | ① | ② | ①−② |
   |---|---|---|---|
   | A | 3 km | 20 km | −17 → 먼저 |
   | C | 8 km | 4 km | +4 → V2에 미룸 |
   | D | 2 km | 뒤차에 못 실림 | **최우선** |

   D처럼 뒤차에 실을 수 있는 차가 하나도 없는 주문은 지금 아니면 영영 못 실으니 계산 없이 최우선. 더 안 들어갈
   때까지 싣고 V1을 닫는다.
3. **V2** — 같은 계산, 이번엔 "뒤차"가 V3뿐. **V3** — 마지막 차라 ②가 없으니 싼 것부터. 남는 주문은 미배정.

"remaining-regret" = **남은** 차들 대비 후회값. H2의 후회값이 자리 2·3순위와의 차이라면 여기서는 뒤차들과의
차이다. 출처는 VROOM의 기본 초기해.

**H3와의 차이.** 둘 다 차 단위인데 H3는 차에 **구역 하나를 통째로**, H14는 **주문 하나씩**. 그래서 H3는 구역이
없으면 힘을 잃고 H14는 구역과 무관하다 — §4.2 표에서 H14가 "구역 없음" 축의 대표가 된 이유.

**왜 남겼나.** ① 차를 하나씩 꽉 채우고 닫으니 "반만 찬 차"가 잘 안 생긴다 — 점수 2번 칸(차량 수)을 초기해에서
잡는 방법이 이것과 H3(구역 의존)뿐. ② ALNS의 repair는 "주문 하나를 가장 싼 자리에"를 반복해 모든 차에 골고루
퍼뜨리는 경향이라 "이 차를 먼저 꽉 채우자"는 판단을 하지 않는다. ③ 실물 5위(56).

**한계.** 차 순서가 "큰 차부터" 고정이라 큰 차가 큰 차 금지 구역에 못 가는 실물 같은 입력에서는 첫 차의 선택지가
좁다. 한 번 닫은 차는 다시 열지 않아 앞차의 실수를 뒤차가 못 고친다(그건 ALNS 몫). 매 삽입마다 뒤차 전부의
"혼자 싣는 비용"을 계산하므로 519 ms.

코드 수준 해설은 [vehicle-fill-remaining-regret 노트](vehicle-fill-remaining-regret-construction.md).

코드 수준 해설은 [vehicle-fill-remaining-regret 노트](vehicle-fill-remaining-regret-construction.md).

### 6.4 H18 — "주문을 한 줄로 세운 다음, 어디서 끊어 차를 나눌지를 계산으로 정한다"

**용어.** "한 줄로 세우기(giant tour)" = 차 구분 없이 모든 주문을 하나의 긴 순서로 — 마치 차 한 대가 452군데를
전부 도는 것처럼. "절단(split)" = 그 줄을 몇 군데서 끊어 조각마다 차 한 대씩. 끊는 위치가 곧 배차다.

**예시.** 주문 8건(A~H), 차 3대.

- **1단계 — 줄 세우기.** 좌표를 힐베르트 곡선 위치(§6.5)로 바꿔 그 순으로 세운다: `C, A, F, B, H, D, G, E`.
  이웃한 주문(C·A, A·F …)은 지도에서도 가깝다. **좌표는 여기까지만** 쓴다.
- **2단계 — 어디서 끊을까.** `[C A F][B H D][G E]`, `[C A][F B H D][G E]`, … 가능한 끊기가 많다. 그중 최선을
  **동적 계획법**으로 정한다 — "앞에서 i번째까지를 차 k대로 처리했을 때의 최선 점수"를 표로 채워 나가고, 각 칸은
  "마지막 조각을 어디서 시작할까"만 골라 앞 칸의 답에 붙인다. 표를 다 채우면 (i=8, k≤3) 칸이 답이고 거꾸로
  따라가면 끊은 위치가 나온다. 조각 하나가 "되는 경로"인지는 어림하지 않고 **실제 계산**(시간표 전파)으로
  확인하며, 어기는 조각은 후보에서 뺀다.
- **3단계 — 점수.** 끊기의 기준은 `[미배정, 차량 수, 거리]`. 어느 조각에도 못 들어가는 주문은 미배정.

**조각 안의 방문 순서 = 줄 세운 순서 그대로**다. `[C A F]`면 `차고 → C → A → F → 차고` — C→F→A가 더 짧아도 바꾸지
않는다(`GiantTourSplit`이 순서대로 `appendPair`). 조각 안 순서를 다시 짜는 건 ALNS 몫이다. 픽업·배송 짝 주문은
줄에서의 위치를 두 지점의 **중점**으로 정하고 조각 안에서 `…, 픽업, 배송, …`으로 **붙어서** 들어간다. 조각은 앞에서부터
**적재량 큰 차 순**으로 받는다 — "이 조각엔 5톤이 필요하다"는 판단을 순서 단계에서 못 하는 한계가 여기서 나온다.

**이 방법만의 성질.** H22·H2·H14는 "다음에 뭘 어디에"를 하나씩 탐욕으로 정하고 되돌리지 않는다. H18은 순서를
고정한 뒤에는 **가능한 모든 끊기 중 최선**을 고른다 — "같은 순서라면 어떤 탐욕적 끊기보다 나쁠 수 없다"는 보장이
있다. 대신 순서 자체는 좌표 규칙일 뿐이라 **순서가 나쁘면 끊기를 아무리 잘해도 소용없다.** H19는 끊기(2단계)가
같고 줄 세우기만 "차고에서 가장 가까운 것 잇기"로 다르다(좌표 불필요, 단일 차고 요구).

**왜 남겼나 — 그리고 왜 "가설"인가.** 실물 20위(165)인데도 남긴 이유는 "구역 없음·좌표 있음" 축의 대표가
필요해서다 — 구역이 없으면 "누구를 같이 실을까"를 방법이 스스로 찾아야 하는데, 지리적 묶음을 만드는 방법은
H18·H19·H20·H8뿐이고 그중 차고 수와 무관하게 항상 도는 건 H18뿐이다. 실물에서 약한 이유는 **가설**이다: 실물은
"한 경로는 구역 1종만" 규칙이 있는데 힐베르트 순서는 구역 경계를 모르고 줄을 세우니, 줄에서 이웃한 두 주문이
다른 구역이면 같은 조각에 못 들어가 조각이 잘게 쪼개진다. 이걸 확인하지 못했으므로 §4.3 (a) "구역을 지운
변형"을 Stage 8로 넘겼다 — 거기서 H18이 크게 오르지 않으면 이 축의 대표를 바꾼다.

코드 수준 해설은 [hilbert-split 노트](hilbert-split-construction.md).

코드 수준 해설은 [hilbert-split 노트](hilbert-split-construction.md).

### 6.5 힐베르트 순서 — "지도를 한 붓으로 훑는 길을 정해 두고, 그 길에서 몇 번째로 만나느냐로 번호를 매긴다"

**왜 필요한가.** 지도는 2차원인데 줄은 1차원이다. "왼쪽 위부터 한 줄씩 읽기"로 세우면 한 줄의 끝과 다음 줄의
시작이 지도에서 정반대 끝이라, 줄에서 이웃인데 실제로는 먼 쌍이 줄마다 생긴다.

```text
책 읽기 순서 (4×4)     힐베르트 순서 (4×4)
 1  2  3  4             1  2 15 16
 5  6  7  8             4  3 14 13
 9 10 11 12             5  8  9 12
13 14 15 16             6  7 10 11
```

왼쪽은 4→5가 점프한다. 오른쪽은 **모든 연속 번호가 상하좌우로 붙어 있다** — 1→2→…→16 어디에도 점프가 없다.

**어떻게 만드나 — 같은 모양을 4번 겹쳐 그린다.** 1차(2×2)는 U자 하나(좌상 → 좌하 → 우하 → 우상). 2차(4×4)는
그 U자를 네 사분면에 하나씩 넣되 한 붓으로 이어지도록 좌상·좌하 조각을 돌려 놓고 좌상 → 좌하 → 우하 → 우상으로
잇는다(위 그림의 1~4 / 5~8 / 9~12 / 13~16). 3차, 4차 … 같은 식. 코드는 **16차** — 65,536 × 65,536 격자.
`hilbertIndex`의 `for (s = side/2; s > 0; s /= 2)`가 "사분면을 16번 내려가며 어느 조각인지"를 세는 부분이고,
`x = side−1−x` 줄들이 조각을 돌려 놓는 부분이다.

**왜 배차에 좋은가.** "곡선에서 가까우면 지도에서도 가깝다"가 모든 차수에서 유지된다 — 곡선상 연속 16개는 하나의
4×4 블록, 연속 64개는 하나의 8×8 블록 안에 있다. 즉 **줄을 어디서 끊어도 그 조각은 지도에서 한 덩어리**다.
H18이 "끊기만 최적으로 하면 된다"고 말할 수 있는 근거다.

| 훑기 방식 | 연속 번호가 붙어 있나 | 문제 |
|---|---|---|
| 책 읽기 | 줄 끝마다 점프 | 줄 수만큼 먼 쌍 |
| 지그재그(뱀) | 붙어 있음 | 조각이 가늘고 긴 띠 — 한 차가 동서로 길게 돈다 |
| Z-곡선(Morton) | 사분면 경계에서 점프 | 힐베르트보다 점프가 많다 |
| **힐베르트** | 항상 붙어 있음 | 조각이 뭉툭한 덩어리 |

**코드가 하는 일 (`hilbertOrder`).** ① 주문마다 대표 좌표 하나(배송 전용은 배송지, 짝 주문은 중점) → ② 전체
주문의 위도·경도 최소~최대로 경계 상자를 만들고 65,536칸 격자에서 어느 칸인지(`normalize`) → ③ 칸 (x, y)를
힐베르트 번호 d(0 ~ 약 43억)로(`hilbertIndex`) → ④ d 오름차순, 같으면 주문 ID 순. 이후 조각의 거리·시간은 전부
이동표에서 읽는다 — 좌표가 대충이어도 순서만 그럴듯하면 되고 정확한 비용은 이동표가 책임진다.

**한계.** 경계 상자는 주문 분포에 따라 늘어난다 — 250 km 밖 주문 하나(ZONE_29)가 있으면 도심 주문들이 격자의
작은 귀퉁이에 몰린다(16차가 충분히 촘촘해 순서 자체는 안 망가진다). 위도 1도와 경도 1도의 실제 길이가 다른데
각각 0~1로 정규화하니 약간의 왜곡이 있다. 사분면 경계 바로 양쪽은 지도에서 100 m여도 곡선 번호가 수억 차이 나
다른 조각에 간다 — 어떤 공간 곡선도 못 피하는 성질이고, 그런 쌍을 한 차에 묶는 건 ALNS가 나중에 한다. 그리고
구역·차종 같은 비지리 규칙은 전혀 모른다 — §6.4 가설의 원인.

인덱스 산수·4×4 표·DP는 [hilbert-split 노트](hilbert-split-construction.md).
